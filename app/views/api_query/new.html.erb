<% provide(:title, "#{params[:search_type]} search - #{params[:keyword]}") %>
<h2>Search for <%= params[:keyword] %></h2>
<ol>
	<% @result.each do |post| %>
		<li> <%= post['text'] %> 
			<div><%= get_sentiment(post['text'])['label'] %> </div>	
		</li>
	<% end %>
</ol>

 

